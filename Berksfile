site :opscode

metadata

cookbook 'rvm', github: 'fnichol/chef-rvm' #, :branch => 'v0.9.2'
cookbook 'yum-epel'
# cookbook 'rvm', git: 'git://github.com/fnichol/chef-rvm.git', :branch => 'v0.7.2'
# cookbook 'rbenv', github: 'fnichol/chef-rbenv' # , branch: 'v0.7.2'
#cookbook 'rbenv', github: 'fnichol/chef-rbenv', branch: 'v0.7.2'
# cookbook 'ruby_build'

#group :integration do
#  cookbook "apt"
#end
